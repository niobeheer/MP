document.querySelectorAll("[data-europe-filter]").forEach(form=>{const query=form.querySelector("[data-europe-query]");const provider=form.querySelector("[data-europe-provider]");const cards=[...document.querySelectorAll("[data-europe-grid] .europe-offer-card")];const count=document.querySelector("[data-europe-count]");const update=()=>{const q=query.value.trim().toLowerCase();const p=provider.value;let visible=0;for(const card of cards){const show=(!q||card.dataset.search.includes(q))&&(!p||card.dataset.provider===p);card.hidden=!show;if(show)visible+=1}count.textContent=`${visible} ${visible===1?"camping":"campings"} zichtbaar`};form.addEventListener("input",update);form.addEventListener("submit",event=>event.preventDefault())});
